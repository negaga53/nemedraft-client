# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Environment — keep .env.client (defaults), ignore generated *.local
.env
.env.*.local
!.env.client

# Build outputs
dist/
build/
out/
*.egg-info/
.cache/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Misc
*.tmp
*.temp

# Scryfall bulk export (regenerated on demand by common.data.scryfall).
# Scryfall publishes gzipped JSON Lines now (~77 MB); the legacy JSON array
# (~550 MB) is still readable if an old one is lying around.
data/scryfall/default_cards.json
data/scryfall/default_cards.jsonl
data/scryfall/default_cards.jsonl.gz

# Card name translation cache — populated on demand from the Scryfall API
client/overlay/i18n/card_cache/
data/cache/draft_state.json
data/cache/pick_history.json
data/card_art_cache/
build/
